const { IdValidator } = require("./IdValidator");
const { addWordValidator } = require("./addWordValidator");
const { updateWordValidator } = require("./updateWordValidator");

module.exports = {
    IdValidator,
    addWordValidator,
    updateWordValidator,
};
